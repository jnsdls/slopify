# Copy to .env.local (ignored by git). The client id is public; the redirect URI is fixed at
# http://127.0.0.1:8888/callback and must be registered on the app in the Spotify dashboard.
MAIN_VITE_SPOTIFY_CLIENT_ID=
